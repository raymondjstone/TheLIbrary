# thelibrary.client

React 19 + Vite frontend for TheLibrary. See the [root README](../README.md) for full project documentation.

## Dev

```bash
npm install
npm run dev   # proxies /api and /hangfire to the backend at localhost:5000
```
